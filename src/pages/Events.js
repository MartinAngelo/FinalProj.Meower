import React from 'react'
import Nav from "../Components/nav";

export default function Messages() {
    return (
        <div>
            <Nav />
            <h1 className="welcome">Events</h1>
            <p class="tag">No Events Available</p>
        </div>
    )
}
